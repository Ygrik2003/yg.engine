#include "forwards.hxx"

int main()
{
    // bot    b1;
    // player p1;

    // b1.recive_damage(p1);
    // p1.recive_damage(b1);
    // p1.recive_damage(b1);
    // p1.recive_damage(b1);
    // p1.recive_damage(b1);

    return EXIT_SUCCESS;
}